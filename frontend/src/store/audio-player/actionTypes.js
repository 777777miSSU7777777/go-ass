const namespace = 'AUDIO_PLAYER';

export const SET_PLAYER = `${namespace}.SET_PLAYER`;
export const PLAY = `${namespace}.PLAY`;
export const RESUME = `${namespace}.RESUME`;
export const PAUSE = `${namespace}.PAUSE`;
export const PREV_TRACK = `${namespace}.PREV_TRACK`;
export const NEXT_TRACK = `${namespace}.NEXT_TRACK`;